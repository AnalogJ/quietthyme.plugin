---
  engine_disable_dist: true
  test_step:
    override:
      - calibre --version
      - calibre-debug --test-build
      - calibre-customize -b .
      - calibre-debug tests/test_quietthymedeviceplugin.py